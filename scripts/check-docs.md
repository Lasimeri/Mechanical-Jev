# scripts/check-docs.sh

`make docs-check`: every code file under `src/` and `scripts/` has a
sibling Markdown file; no em or en dash anywhere tracked; every relative
Markdown link points at a file that exists.
